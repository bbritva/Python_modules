class Vector:
    def __init__(self, values):
        self.values = values
        self.shape = (len(values), len(values[0]))
    def __init__(self, values):
        self.values = values
        self.shape = (len(values), len(values[0]))

